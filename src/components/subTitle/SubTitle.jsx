import React from 'react';

const SubTitle = ({text}) => {
    return (
        <span className='bg-[rgba(0,0,0,.05)] text-slate-900 font-medium uppercase text-sm p-[10px]'>{text}</span>
    );
};

export default SubTitle;